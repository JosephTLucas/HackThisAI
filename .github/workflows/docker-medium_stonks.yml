name: Docker Image CI - medium_stonks

on:
  push:
    branches: [ main ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker build . --file challenge/medium_stonks/Dockerfile --tag stonks:$(date +%s)