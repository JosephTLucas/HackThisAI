name: Docker Image CI - easy_credit_check

on:
  push:
    branches: [ main ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker build . --file challenge/easy_credit_check/Dockerfile --tag credit_check:$(date +%s)