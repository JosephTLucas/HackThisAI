name: Docker Image CI - medium_flying_pig

on:
  push:
    branches: [ main ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker build . --file challenge/medium_flying_pig/Dockerfile --tag flying_pig:$(date +%s)