name: Docker Image CI - medium_rob_vader

on:
  push:
    branches: [ main ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker build . --file challenge/medium_rob_vader/Dockerfile --tag vader:$(date +%s)